## ShutDown v2.0.0

### 重大调整

- 将界面从 Qt 重构为原生 Win32++ 实现，降低运行依赖。
- 修复 WinHTTP 更新器在 Win32 API 调用和 TLS 协议兼容性上的问题。
- 更新 Win32xx 构建说明，并启用兼容旧系统的 TLS 协议配置。
- 继续提供 Windows x86/x64 架构发布包。

### 发布文件

- `ShutDown-windows-x64.zip`：Windows 64 位。
- `ShutDown-windows-x86.zip`：Windows 32 位。
