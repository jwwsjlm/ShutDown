## ShutDown v2.0.3

### 更新检查调整

- 移除仓库内 `update.json` 更新描述文件。
- 更新器直接读取 GitHub Releases API。
- API 受限或限流时，自动回退到 GitHub Release 页面解析最新版本和架构资产。
- 继续根据当前程序位数选择 x86/x64 ZIP，并进行 SHA-256 校验。

### 发布文件

- `ShutDown-windows-x64.zip`：Windows 64 位。
- `ShutDown-windows-x86.zip`：Windows 32 位。
