## ShutDown v2.0.4

### 定时输入改进

- 开始倒计时前，如果距离关机时间不超过 10 分钟，弹窗要求用户确认，避免误填导致马上关机。
- 倒计时成功后，上方日期时间框会自动显示预计关机时间，便于核对。
- 倒计时为 0 秒时不再进入调度流程，而是直接提示输入无效。

### 发布文件

- `ShutDown-windows-x64.zip`：Windows 64 位。
- `ShutDown-windows-x86.zip`：Windows 32 位。
